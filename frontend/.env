# In production, the React frontend is served by the same Node.js server,
# so relative paths (/api) and window.location.origin are used automatically.
# Uncomment and set this only if hosting the frontend on a separate domain.
# VITE_API_URL=https://ipl-auction-backend.onrender.com
