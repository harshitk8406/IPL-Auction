{
  "name": "ipl-auction-app",
  "version": "1.0.0",
  "description": "IPL Auction Full Stack App",
  "scripts": {
    "setup": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "node backend/server.js"
  }
}
